// Dibuja un texto en la pantalla
function drawText(message, x, y) {
    ctx.font = "30px Arial";
    ctx.fillStyle = "red";
    ctx.textAlign = "right";
    ctx.fillText("show this on the screen", 0, 0);
}