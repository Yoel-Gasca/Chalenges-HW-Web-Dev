// Funcion que suma 2 numeros
const add = (a, b) => a + b;
console.log(add(1, 2)); // 3

// Funcion que resta 2 numeros
const subtract = (a, b) => { // Las llaves se pueden utilizar cuando se introducen varias lineas de codigo
    return a - b
};
console.log(subtract(2, 1)); // 1